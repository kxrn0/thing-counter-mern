import styled from "styled-components";

const SCCounter = styled.div`
  background: #62ff00;
  flex-grow: 1;

  .image {
    img {
      max-width: 100vw;
      max-height: 100vh;
    }
  }  
`;

export default SCCounter;
